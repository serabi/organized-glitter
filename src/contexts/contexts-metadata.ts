/**
 * Metadata context definition - legacy compatibility layer
 * @deprecated Use @/contexts/MetadataContext directly for new code
 * @author @serabi
 * @created 2025-08-02
 */

// Re-export from the new MetadataContext folder for backward compatibility
export { MetadataContext, type MetadataContextType } from './MetadataContext/context';
